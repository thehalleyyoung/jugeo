name: widget
count: 2
active: true
EOF 2>/dev/null || true
OUTPUT=$($TOOL convert --input note.yaml --to json --pretty 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "\"name\": \"widget\""; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: \"name\": \"widget\")"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert_yaml_to_json_stdout: stdout missing '\"name\": \"widget\"'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert_yaml_to_json_stdout: exit=$EXIT_CODE expected=0"
fi

# Test 2: convert_json_to_toml_file
echo -n "  [2/13] convert_json_to_toml_file... "
TOTAL=$((TOTAL + 1))
printf '{"service":{"host":"localhost","port":8080}}\n' > app.json 2>/dev/null || true
OUTPUT=$($TOOL convert --input app.json --to toml --output app.toml 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "app.toml"; then
    if [ -f "app.toml" ]; then
      echo "PASS"
      PASSED=$((PASSED + 1))
    else
      echo "FAIL (file missing: app.toml)"
      FAILED=$((FAILED + 1))
      FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert_json_to_toml_file: file missing app.toml"
    fi
  else
    echo "FAIL (stdout missing: app.toml)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert_json_to_toml_file: stdout missing 'app.toml'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert_json_to_toml_file: exit=$EXIT_CODE expected=0"
fi

# Test 3: convert_toml_to_yaml_from_stdin
echo -n "  [3/13] convert_toml_to_yaml_from_stdin... "
TOTAL=$((TOTAL + 1))
printf 'title = "sample"\n[server]\nport = 8080\n' > conf.toml 2>/dev/null || true
OUTPUT=$(cat conf.toml | $TOOL convert --from toml --to yaml 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "title: sample"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: title: sample)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert_toml_to_yaml_from_stdin: stdout missing 'title: sample'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert_toml_to_yaml_from_stdin: exit=$EXIT_CODE expected=0"
fi

# Test 4: auto_detect_json_without_from_flag
echo -n "  [4/13] auto_detect_json_without_from_flag... "
TOTAL=$((TOTAL + 1))
printf '{"enabled":true,"retries":3}\n' > auto.json 2>/dev/null || true
OUTPUT=$($TOOL convert --input auto.json --to yaml 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "enabled: true"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: enabled: true)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL auto_detect_json_without_from_flag: stdout missing 'enabled: true'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL auto_detect_json_without_from_flag: exit=$EXIT_CODE expected=0"
fi

# Test 5: convert_with_pretty_sorted_json
echo -n "  [5/13] convert_with_pretty_sorted_json... "
TOTAL=$((TOTAL + 1))
cat > order.yaml <<'EOF'
b: 2
a: 1
EOF 2>/dev/null || true
OUTPUT=$($TOOL convert --input order.yaml --to json --pretty --sort-keys 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "\"a\": 1"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: \"a\": 1)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert_with_pretty_sorted_json: stdout missing '\"a\": 1'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert_with_pretty_sorted_json: exit=$EXIT_CODE expected=0"
fi

# Test 6: validate_yaml_file
echo -n "  [6/13] validate_yaml_file... "
TOTAL=$((TOTAL + 1))
cat > valid.yaml <<'EOF'
name: demo
items:
  - one
  - two
EOF 2>/dev/null || true
OUTPUT=$($TOOL validate --input valid.yaml 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "valid yaml"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: valid yaml)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL validate_yaml_file: stdout missing 'valid yaml'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL validate_yaml_file: exit=$EXIT_CODE expected=0"
fi

# Test 7: validate_toml_from_stdin
echo -n "  [7/13] validate_toml_from_stdin... "
TOTAL=$((TOTAL + 1))
printf 'name = "demo"\n[build]\noptimized = true\n' > valid.toml 2>/dev/null || true
OUTPUT=$(cat valid.toml | $TOOL validate --from toml 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "valid toml"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: valid toml)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL validate_toml_from_stdin: stdout missing 'valid toml'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL validate_toml_from_stdin: exit=$EXIT_CODE expected=0"
fi

# Test 8: inspect_top_level_keys
echo -n "  [8/13] inspect_top_level_keys... "
TOTAL=$((TOTAL + 1))
cat > service.yaml <<'EOF'
service:
  host: localhost
  port: 8080
version: 1
EOF 2>/dev/null || true
OUTPUT=$($TOOL inspect --input service.yaml 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "top-level keys: service, version"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: top-level keys: service, version)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL inspect_top_level_keys: stdout missing 'top-level keys: service, version'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL inspect_top_level_keys: exit=$EXIT_CODE expected=0"
fi

# Test 9: inspect_dotted_query_value
echo -n "  [9/13] inspect_dotted_query_value... "
TOTAL=$((TOTAL + 1))
cat > query.yaml <<'EOF'
service:
  host: localhost
  port: 8080
EOF 2>/dev/null || true
OUTPUT=$($TOOL inspect --input query.yaml --query service.port 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "8080"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: 8080)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL inspect_dotted_query_value: stdout missing '8080'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL inspect_dotted_query_value: exit=$EXIT_CODE expected=0"
fi

# Test 10: merge_yaml_overrides_stdout
echo -n "  [10/13] merge_yaml_overrides_stdout... "
TOTAL=$((TOTAL + 1))
cat > base.yaml <<'EOF'
service:
  port: 8080
  host: localhost
EOF 2>/dev/null || true
cat > override.yaml <<'EOF'
service:
  port: 9090
EOF 2>/dev/null || true
OUTPUT=$($TOOL merge --to yaml base.yaml override.yaml 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "port: 9090"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: port: 9090)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL merge_yaml_overrides_stdout: stdout missing 'port: 9090'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL merge_yaml_overrides_stdout: exit=$EXIT_CODE expected=0"
fi

# Test 11: merge_deep_preserves_nested_values
echo -n "  [11/13] merge_deep_preserves_nested_values... "
TOTAL=$((TOTAL + 1))
printf '{"service":{"host":"localhost","port":8080,"retries":3}}\n' > base.json 2>/dev/null || true
printf '[service]\nport = 9001\n' > override.toml 2>/dev/null || true
OUTPUT=$($TOOL merge --to json base.json override.toml --pretty 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "\"retries\": 3"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: \"retries\": 3)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL merge_deep_preserves_nested_values: stdout missing '\"retries\": 3'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL merge_deep_preserves_nested_values: exit=$EXIT_CODE expected=0"
fi

# Test 12: demo_stdout_without_input_files
echo -n "  [12/13] demo_stdout_without_input_files... "
TOTAL=$((TOTAL + 1))
OUTPUT=$($TOOL demo --to json --variant basic --pretty 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "\"demo\":"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: \"demo\":)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL demo_stdout_without_input_files: stdout missing '\"demo\":'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL demo_stdout_without_input_files: exit=$EXIT_CODE expected=0"
fi

# Test 13: demo_writes_output_file
echo -n "  [13/13] demo_writes_output_file... "
TOTAL=$((TOTAL + 1))
rm -f demo.yaml 2>/dev/null || true
OUTPUT=$($TOOL demo --to yaml --variant nested --output demo.yaml 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "demo.yaml"; then
    if [ -f "demo.yaml" ]; then
      echo "PASS"
      PASSED=$((PASSED + 1))
    else
      echo "FAIL (file missing: demo.yaml)"
      FAILED=$((FAILED + 1))
      FAIL_DETAILS="$FAIL_DETAILS\n  FAIL demo_writes_output_file: file missing demo.yaml"
    fi
  else
    echo "FAIL (stdout missing: demo.yaml)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL demo_writes_output_file: stdout missing 'demo.yaml'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL demo_writes_output_file: exit=$EXIT_CODE expected=0"
fi

echo "=========================================="
echo "Results: $PASSED/$TOTAL passed, $FAILED failed"
if [ "$FAILED" -gt 0 ]; then
  echo ""
  echo "Failure details:"
  echo -e "$FAIL_DETAILS"
  exit 1
fi

name: app
port: 8080
debug: true
EOF 2>/dev/null || true
OUTPUT=$($TOOL convert --from yaml --to json --input config.yaml --output config.json 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if [ -f "config.json" ]; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (file missing: config.json)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert-yaml-file-to-json-file: file missing config.json"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert-yaml-file-to-json-file: exit=$EXIT_CODE expected=0"
fi

# Test 2: convert-json-stdin-to-yaml-stdout
echo -n "  [2/12] convert-json-stdin-to-yaml-stdout... "
TOTAL=$((TOTAL + 1))
cat > data.json <<'EOF'
{"service":"api","replicas":3}
EOF 2>/dev/null || true
OUTPUT=$(cat data.json | $TOOL convert --from json --to yaml 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "service: api"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: service: api)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert-json-stdin-to-yaml-stdout: stdout missing 'service: api'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert-json-stdin-to-yaml-stdout: exit=$EXIT_CODE expected=0"
fi

# Test 3: convert-toml-to-json-with-sorted-keys
echo -n "  [3/12] convert-toml-to-json-with-sorted-keys... "
TOTAL=$((TOTAL + 1))
cat > app.toml <<'EOF'
version = "1.2.0"
name = "worker"
EOF 2>/dev/null || true
OUTPUT=$($TOOL convert --from toml --to json --input app.toml --sort-keys 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "\"name\""; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: \"name\")"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert-toml-to-json-with-sorted-keys: stdout missing '\"name\"'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL convert-toml-to-json-with-sorted-keys: exit=$EXIT_CODE expected=0"
fi

# Test 4: validate-valid-yaml-file
echo -n "  [4/12] validate-valid-yaml-file... "
TOTAL=$((TOTAL + 1))
cat > valid.yaml <<'EOF'
items:
  - a
  - b
EOF 2>/dev/null || true
OUTPUT=$($TOOL validate --from yaml --input valid.yaml 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "valid"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: valid)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL validate-valid-yaml-file: stdout missing 'valid'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL validate-valid-yaml-file: exit=$EXIT_CODE expected=0"
fi

# Test 5: validate-auto-detected-toml-file
echo -n "  [5/12] validate-auto-detected-toml-file... "
TOTAL=$((TOTAL + 1))
cat > settings.toml <<'EOF'
[server]
port = 9000
EOF 2>/dev/null || true
OUTPUT=$($TOOL validate --from auto --input settings.toml --show-format 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "toml"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: toml)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL validate-auto-detected-toml-file: stdout missing 'toml'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL validate-auto-detected-toml-file: exit=$EXIT_CODE expected=0"
fi

# Test 6: inspect-json-summary
echo -n "  [6/12] inspect-json-summary... "
TOTAL=$((TOTAL + 1))
cat > package.json <<'EOF'
{"name":"demo","version":"0.1.0","scripts":{"start":"python app.py"}}
EOF 2>/dev/null || true
OUTPUT=$($TOOL inspect --from json --input package.json 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "top_level_type"; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: top_level_type)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL inspect-json-summary: stdout missing 'top_level_type'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL inspect-json-summary: exit=$EXIT_CODE expected=0"
fi

# Test 7: inspect-yaml-as-json-output
echo -n "  [7/12] inspect-yaml-as-json-output... "
TOTAL=$((TOTAL + 1))
cat > info.yaml <<'EOF'
owner: ops
enabled: true
regions:
  - us-east-1
  - eu-west-1
EOF 2>/dev/null || true
OUTPUT=$($TOOL inspect --input info.yaml --json 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "\"detected_format\""; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: \"detected_format\")"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL inspect-yaml-as-json-output: stdout missing '\"detected_format\"'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL inspect-yaml-as-json-output: exit=$EXIT_CODE expected=0"
fi

# Test 8: format-json-in-place
echo -n "  [8/12] format-json-in-place... "
TOTAL=$((TOTAL + 1))
printf '{"b":2,"a":1}' > messy.json 2>/dev/null || true
OUTPUT=$($TOOL format --input messy.json --in-place --indent 2 --sort-keys 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if [ -f "messy.json" ]; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (file missing: messy.json)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL format-json-in-place: file missing messy.json"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL format-json-in-place: exit=$EXIT_CODE expected=0"
fi

# Test 9: format-yaml-to-toml-output-file
echo -n "  [9/12] format-yaml-to-toml-output-file... "
TOTAL=$((TOTAL + 1))
cat > deploy.yaml <<'EOF'
image: web:latest
replicas: 2
EOF 2>/dev/null || true
OUTPUT=$($TOOL format --from yaml --to toml --input deploy.yaml --output deploy.toml 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if [ -f "deploy.toml" ]; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (file missing: deploy.toml)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL format-yaml-to-toml-output-file: file missing deploy.toml"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL format-yaml-to-toml-output-file: exit=$EXIT_CODE expected=0"
fi

# Test 10: batch-convert-yaml-directory-to-json
echo -n "  [10/12] batch-convert-yaml-directory-to-json... "
TOTAL=$((TOTAL + 1))
mkdir -p src_configs/nested 2>/dev/null || true
cat > src_configs/app.yaml <<'EOF'
name: frontend
EOF 2>/dev/null || true
cat > src_configs/nested/job.yml <<'EOF'
cron: "0 0 * * *"
enabled: true
EOF 2>/dev/null || true
OUTPUT=$($TOOL batch --from auto --to json --input-dir src_configs --output-dir out_configs --glob '*.y*ml' --recursive --overwrite 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "converted"; then
    if [ -f "out_configs/app.json" ]; then
      echo "PASS"
      PASSED=$((PASSED + 1))
    else
      echo "FAIL (file missing: out_configs/app.json)"
      FAILED=$((FAILED + 1))
      FAIL_DETAILS="$FAIL_DETAILS\n  FAIL batch-convert-yaml-directory-to-json: file missing out_configs/app.json"
    fi
  else
    echo "FAIL (stdout missing: converted)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL batch-convert-yaml-directory-to-json: stdout missing 'converted'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL batch-convert-yaml-directory-to-json: exit=$EXIT_CODE expected=0"
fi

# Test 11: demo-stdout-without-input-files
echo -n "  [11/12] demo-stdout-without-input-files... "
TOTAL=$((TOTAL + 1))
OUTPUT=$($TOOL demo --scenario basic --from yaml --to json 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "\"name\""; then
    echo "PASS"
    PASSED=$((PASSED + 1))
  else
    echo "FAIL (stdout missing: \"name\")"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL demo-stdout-without-input-files: stdout missing '\"name\"'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL demo-stdout-without-input-files: exit=$EXIT_CODE expected=0"
fi

# Test 12: demo-write-sample-files
echo -n "  [12/12] demo-write-sample-files... "
TOTAL=$((TOTAL + 1))
mkdir -p demo_out 2>/dev/null || true
OUTPUT=$($TOOL demo --scenario ci-config --from toml --write-dir demo_out 2>&1) && EXIT_CODE=$? || EXIT_CODE=$?
if [ "$EXIT_CODE" -eq 0 ]; then
  if echo "$OUTPUT" | grep -qi "demo_out"; then
    if [ -f "demo_out/ci-config.toml" ]; then
      echo "PASS"
      PASSED=$((PASSED + 1))
    else
      echo "FAIL (file missing: demo_out/ci-config.toml)"
      FAILED=$((FAILED + 1))
      FAIL_DETAILS="$FAIL_DETAILS\n  FAIL demo-write-sample-files: file missing demo_out/ci-config.toml"
    fi
  else
    echo "FAIL (stdout missing: demo_out)"
    FAILED=$((FAILED + 1))
    FAIL_DETAILS="$FAIL_DETAILS\n  FAIL demo-write-sample-files: stdout missing 'demo_out'"
  fi
else
  echo "FAIL (exit code: $EXIT_CODE, expected: 0)"
  FAILED=$((FAILED + 1))
  FAIL_DETAILS="$FAIL_DETAILS\n  FAIL demo-write-sample-files: exit=$EXIT_CODE expected=0"
fi

echo "=========================================="
echo "Results: $PASSED/$TOTAL passed, $FAILED failed"
if [ "$FAILED" -gt 0 ]; then
  echo ""
  echo "Failure details:"
  echo -e "$FAIL_DETAILS"
  exit 1
fi
